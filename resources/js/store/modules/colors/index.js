import getters from './getters';

const state = {
    colors: [
        'primary',
        'neutral',
        'info',
        'success',
        'warning',
        'error'
    ],
}

export default {
    getters,
    state
}
